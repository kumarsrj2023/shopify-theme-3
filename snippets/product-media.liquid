{% case media.media_type %}
  {% when 'image' %}
    <img src="{{ media | img_url: 'medium' }}" width="" height="" alt="{{ media.image.alt }}" class="w-full h-full object-cover">
  {% else %}

{% endcase %}
